// declare an array 
var arrayName = [45, 86, 34, 87, 12];

// all elements value 
for (var i = 0; i < arrayName.length; i++) {
    var element = arrayName[i];
    console.log(element);
}


// object declaration 
const objectName = {
    arrayProperty: ['values1', 'values2', 'values3', '...'],
    normalProperty1: 'values',
    normalProperty2: 'values',
    normalProperty3: 'Values',
}